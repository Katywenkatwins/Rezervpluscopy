# ✅ ЧЕКЛИСТ ДЕПЛОЮ - Що перевірити

## 🔍 **Перевірка 1: GitHub Actions**

### Перейдіть на:
https://github.com/Katywenkatwins/Rezervplus/actions

### Що шукати:
- ✅ Зелена галочка - build успішний
- ❌ Червоний хрестик - build провалився
- 🟡 Жовте коло - build виконується

### Якщо червоний хрестик:
1. Клікніть на workflow
2. Клікніть на "build" job
3. Дивіться логи - там буде помилка
4. Напишіть мені помилку - я виправлю

---

## 🔍 **Перевірка 2: GitHub Pages налаштування**

### Перейдіть на:
https://github.com/Katywenkatwins/Rezervplus/settings/pages

### Має бути:
```
Source: GitHub Actions ✅
(НЕ "Deploy from a branch")
```

### Якщо інше:
1. Змініть на "GitHub Actions"
2. Збережіть
3. Зачекайте 2-3 хвилини

---

## 🔍 **Перевірка 3: Структура файлів на GitHub**

### Перейдіть на:
https://github.com/Katywenkatwins/Rezervplus

### Має бути така структура:
```
Rezervplus/
├── .github/
│   └── workflows/
│       └── deploy.yml      ← ОБОВ'ЯЗКОВО!
├── public/
│   ├── manifest.json       ← Оновлений
│   ├── service-worker.js
│   ├── favicon.svg
│   └── icon-*.png          ← Іконки (якщо є)
├── src/
│   ├── App.tsx
│   ├── main.tsx
│   └── components/
├── index.html              ← Має шляхи з /Rezervplus/
├── vite.config.ts          ← base: '/Rezervplus/'
└── package.json
```

---

## 🔍 **Перевірка 4: Вміст файлів**

### Перевірте `vite.config.ts` на GitHub:
```typescript
export default defineConfig({
  plugins: [react()],
  base: '/Rezervplus/',      // ← МАЄ БУТИ!
  build: {
    outDir: 'dist',          // ← МАЄ БУТИ!
  },
  publicDir: 'public',       // ← МАЄ БУТИ!
});
```

### Перевірте `index.html`:
```html
<link rel="manifest" href="/Rezervplus/manifest.json">
<!-- ↑ МАЄ БУТИ /Rezervplus/ -->
```

### Перевірте `public/manifest.json`:
```json
{
  "start_url": "/Rezervplus/",    // ← МАЄ БУТИ!
  "scope": "/Rezervplus/",        // ← МАЄ БУТИ!
  "icons": [
    {
      "src": "/Rezervplus/icon-placeholder.svg"  // ← МАЄ БУТИ!
    }
  ]
}
```

---

## 🔍 **Перевірка 5: Відкрити сайт**

### Відкрийте:
https://katywenkatwins.github.io/Rezervplus/

### Має бути:
- ✅ Сторінка відкривається
- ✅ Видно картку Резерв ID
- ✅ Немає помилок в консолі (F12 → Console)

### Якщо помилки:
1. F12 → Console
2. Скопіюйте помилки
3. Напишіть мені - я виправлю

---

## 🔍 **Перевірка 6: Перевірити Manifest**

### В DevTools:
1. F12 → Application → Manifest
2. Має з'явитися інформація про додаток
3. Перевірте іконки - мають завантажитися

### Якщо помилка "Manifest not found":
- Перевірте шлях в `index.html`
- Має бути: `href="/Rezervplus/manifest.json"`

---

## 🔍 **Перевірка 7: Перевірити файли безпосередньо**

### Спробуйте відкрити в новій вкладці:

```
https://katywenkatwins.github.io/Rezervplus/manifest.json
https://katywenkatwins.github.io/Rezervplus/favicon.svg
https://katywenkatwins.github.io/Rezervplus/icon-placeholder.svg
```

### Якщо 404:
- Файли не задеплоїлись
- Перевірте GitHub Actions (крок 1)
- Перевірте що файли є в папці `public/`

---

## 🚨 **Типові проблеми і рішення**

### ❌ Білий екран / порожня сторінка
**Причина:** Неправильний base path
**Рішення:** 
1. Перевірте `vite.config.ts` → `base: '/Rezervplus/'`
2. Перевірте `index.html` → шляхи з `/Rezervplus/`
3. Запустіть build заново

### ❌ 404 на /Rezervplus/assets/
**Причина:** Build не виконався або не задеплоївся
**Рішення:**
1. Перевірте GitHub Actions
2. Якщо build провалився - дивіться логи
3. Якщо build успішний - зачекайте 2-3 хвилини

### ❌ Manifest not found
**Причина:** Неправильні шляхи
**Рішення:**
1. `index.html` → `href="/Rezervplus/manifest.json"`
2. `manifest.json` → `start_url: "/Rezervplus/"`

### ❌ Icons not loading
**Причина:** Іконок немає або неправильні шляхи
**Рішення:**
1. Завантажте іконки в `public/`
2. В `manifest.json` → `"src": "/Rezervplus/icon-192x192.png"`

---

## ⚡ **ШВИДКЕ ВИПРАВЛЕННЯ**

Якщо щось не працює:

### 1. Перевірте GitHub Actions:
```
Actions → Останній workflow → Дивіться статус
```

### 2. Якщо build провалився:
```
Клікніть на workflow → build → Дивіться логи → Знайдіть помилку
```

### 3. Типові помилки в логах:

| Помилка в логах | Рішення |
|----------------|---------|
| `Cannot find module 'vite'` | Додати в `package.json` → `devDependencies` |
| `Failed to resolve entry` | Перевірити `index.html` → `<script src="/src/main.tsx">` |
| `public/manifest.json not found` | Перевірити що файл є в папці `public/` |
| `Permission denied` | Перевірити Settings → Actions → Permissions |

---

## ✅ **Після успішного деплою**

### Має бути:
- ✅ Сайт відкривається: https://katywenkatwins.github.io/Rezervplus/
- ✅ DevTools → Console - немає червоних помилок
- ✅ DevTools → Application → Manifest - завантажився
- ✅ Картка Резерв ID відображається
- ✅ Всі елементи інтерфейсу працюють

### PWA функціонал:
- ✅ На мобільному: "Add to Home Screen" працює
- ✅ Офлайн режим працює (після першого завантаження)
- ✅ Іконка додатку правильна

---

## 📞 **Якщо нічого не працює**

### Надішліть мені:
1. Скріншот GitHub Actions (зелена галочка чи червоний хрестик)
2. Скріншот DevTools → Console (помилки)
3. Скріншот DevTools → Application → Manifest
4. URL який ви відкриваєте

Я швидко виправлю проблему! 🚀
